mapserver:
    image: neogeo/mapserver 
    volumes:
        - ./maps:/var/maps
    ports:
        - "8080:80"
mapserver2:
    image: neogeo/mapserver
    volumes:
        - ./maps:/var/maps
    ports:
        - "8081:80"

